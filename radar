#!/usr/bin/env bash
# Atajo para no tener que acordarse de la ruta del entorno virtual.
set -euo pipefail
cd "$(dirname "$0")"

if [ ! -x .venv/bin/python ]; then
  echo "El entorno no está instalado. Ejecuta primero:  ./instalar.sh"
  exit 1
fi

exec ./.venv/bin/python -m agor.cli "$@"
